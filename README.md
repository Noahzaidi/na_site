# noahark.org

Website for NoahArk, built with Next.js and deployed to GitHub Pages.

```bash
npm install
npm run dev     # http://localhost:3000
npm run build   # static export to out/
```

Site copy lives in `content/site.ts`, legal details in `content/legal.ts`. Pushing to `main` deploys through GitHub Actions.
